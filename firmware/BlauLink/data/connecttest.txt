void handleConnectTest() {
  servidor.send(200, "text/plain", "OK");
}

void setup() {
  // ... Connexió WiFi i muntatge LittleFS

  servidor.on("/connecttest.txt", handleConnectTest);
  servidor.on("/", handleWeb);
  servidor.begin();
}
